import React, { Fragment, useEffect } from 'react';
import { SafeAreaView, ScrollView, View } from 'react-native';
import { Text } from 'react-native-paper';

export default function ProjectFilter({ navigation, ...props }) {
  return (
    <SafeAreaView>
      <View></View>
    </SafeAreaView>
  );
}
